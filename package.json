{
  "name": "srannyhub-keys",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy -c wrangler.prod.toml",
    "upload-script": "wrangler kv key put -c wrangler.prod.toml --binding=KEYS --remote script --path=script/SrannyHub.lua"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
