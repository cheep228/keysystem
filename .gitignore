node_modules/
.wrangler/
.dev.vars
.env
# сам хаб не публикуем — он лежит только на сервере
script/*.lua
# боевой loader с реальным доменом и данные ключей — только локально/на сервере
loader/*.prod.lua
data/
loader/*.local.lua
