node_modules/
.wrangler/
.dev.vars
.env
# сам хаб не публикуем — он загружается в KV через npm run upload-script
script/*.lua
# реальный домен и боевой loader — только локально
wrangler.prod.toml
loader/*.prod.lua
